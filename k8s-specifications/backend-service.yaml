apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
  - name: "backend-service"
    port: 5000
    targetPort: 5000
  selector: 
    app: backend