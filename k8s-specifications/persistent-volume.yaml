apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongo-persistent
  labels:
    app: mongo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: gp2    
  resources:
    requests:
      storage: 10Gi
